<article class="container" style="padding: 120px 0; min-height: 40vh;">
  <p class="eyebrow">Journal</p>
  <h1 class="page-hero__title">{{ article.title }}</h1>
  <div style="margin-top: 32px;">{{ article.content }}</div>
</article>
