{%- comment -%}
  Shopify requires the gift-card template to stay Liquid (it refuses a JSON
  template here), so this renders the designed section statically. The section
  still shows up in the theme editor with all of its settings.
{%- endcomment -%}
{% section 'main-gift-card' %}
