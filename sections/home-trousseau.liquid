{%- render 'section-design', section: section -%}
{%- liquid
  assign cfg = section.settings
  assign sel = '#shopify-section-' | append: section.id

  # Every card element is hidden with CSS rather than removed in Liquid: the
  # inline script rebuilds this grid from window.SHRUJAN.PIECES the moment a
  # filter is pressed, so a Liquid condition would only hold for the first paint.
  assign has_layout = false
  if cfg.tr_cols != 5 or cfg.tr_cols_m != 2 or cfg.tr_ratio != 'default'
    assign has_layout = true
  endif
  if cfg.tr_show_badge == false or cfg.tr_show_price == false or cfg.tr_show_was == false
    assign has_layout = true
  endif
  if cfg.tr_show_swatches == false or cfg.tr_show_wish == false or cfg.tr_show_quick == false
    assign has_layout = true
  endif
-%}
{%- if has_layout -%}
  <style>
    {%- if cfg.tr_cols != 5 -%}
      @media (min-width: 1081px) {
        {{ sel }} .curated__grid { grid-template-columns: repeat({{ cfg.tr_cols }}, minmax(0, 1fr)); }
      }
    {%- endif -%}
    {%- if cfg.tr_cols_m != 2 -%}
      @media (max-width: 720px) {
        {{ sel }} .curated__grid { grid-template-columns: repeat({{ cfg.tr_cols_m }}, minmax(0, 1fr)); }
      }
    {%- endif -%}
    {%- if cfg.tr_ratio != 'default' -%}
      {{ sel }} .piece__media { aspect-ratio: {{ cfg.tr_ratio }}; }
    {%- endif -%}
    {%- if cfg.tr_show_badge == false -%}
      {{ sel }} .piece__badge { display: none; }
    {%- endif -%}
    {%- if cfg.tr_show_price == false -%}
      {{ sel }} .piece__price { display: none; }
    {%- endif -%}
    {%- if cfg.tr_show_was == false -%}
      {{ sel }} .piece__price span { display: none; }
    {%- endif -%}
    {%- if cfg.tr_show_swatches == false -%}
      {{ sel }} .piece__swatches { display: none; }
    {%- endif -%}
    {%- if cfg.tr_show_wish == false -%}
      {{ sel }} .piece__wish { display: none; }
    {%- endif -%}
    {%- if cfg.tr_show_quick == false -%}
      {{ sel }} .piece__quick { display: none; }
    {%- endif -%}
  </style>
{%- endif -%}
<section class="section container" id="curated">
  <div class="section-intro" data-reveal>
    <p class="eyebrow">{{ section.settings.eyebrow }}</p>
    <h2 class="section-title">{{ section.settings.title }}</h2>
    <p class="section-intro__copy">
      {{ section.settings.intro }}
    </p>
  </div>

  {% if cfg.tr_show_filters %}
  <div class="curated__filters" data-reveal role="tablist" aria-label="Filter by occasion">
    <button type="button" role="tab" aria-selected="true" class="curated__filter is-active">{{ section.settings.tab_1 }}</button>
    <button type="button" role="tab" aria-selected="false" class="curated__filter">{{ section.settings.tab_2 }}</button>
    <button type="button" role="tab" aria-selected="false" class="curated__filter">{{ section.settings.tab_3 }}</button>
    <button type="button" role="tab" aria-selected="false" class="curated__filter">{{ section.settings.tab_4 }}</button>
    <button type="button" role="tab" aria-selected="false" class="curated__filter">{{ section.settings.tab_5 }}</button>
  </div>
  {% endif %}

  <div class="curated__grid" data-reveal-child>
    <a class="piece" href="/pages/shop-shrujan#catalogue">
      <div class="piece__media">
        <img src="{{ 'sj-black-pakko-hand-embroidered-kanchli-1.webp' | asset_url }}" alt="Model wearing the black pakko hand embroidered kanchli" loading="lazy">
        <button class="piece__wish" type="button" aria-label="Save Black Pakko Hand Embroidered Kanchli to wishlist">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>
        </button>
        <span class="piece__quick">Add to bag</span>
      </div>

      <div class="piece__info">
        <p class="piece__name">Black Pakko Hand Embroidered Kanchli</p>
        <p class="piece__craft">Pakko · Kutch</p>
        <p class="piece__price">₹ 25,000</p>
        <span class="piece__swatches" aria-hidden="true">
          <i style="background: #241f1f"></i><i style="background: #c7a56a"></i><i style="background: #f2ede0"></i>
        </span>
      </div>
    </a>
    <a class="piece" href="/pages/shop-shrujan#catalogue">
      <div class="piece__media">
        <img src="{{ 'sj-coffe-brown-hand-embroidered-kanchli-1.webp' | asset_url }}" alt="Model wearing the coffe brown hand embroidered kanchli" loading="lazy">
        <button class="piece__wish" type="button" aria-label="Save Coffe Brown Hand Embroidered Kanchli to wishlist">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>
        </button>
        <span class="piece__quick">Add to bag</span>
      </div>

      <div class="piece__info">
        <p class="piece__name">Coffe Brown Hand Embroidered Kanchli</p>
        <p class="piece__craft">Kutchi hand-embroidery</p>
        <p class="piece__price">₹ 28,000</p>
        <span class="piece__swatches" aria-hidden="true">
          <i style="background: #5b4033"></i><i style="background: #c7a56a"></i><i style="background: #f2ede0"></i>
        </span>
      </div>
    </a>
    <a class="piece" href="/pages/shop-shrujan#catalogue">
      <div class="piece__media">
        <img src="{{ 'sj-turquoise-hand-embroidered-kanchli-1.webp' | asset_url }}" alt="Model wearing the turquoise hand embroidered kanchli" loading="lazy">
        <button class="piece__wish" type="button" aria-label="Save Turquoise Hand Embroidered Kanchli to wishlist">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>
        </button>
        <span class="piece__quick">Add to bag</span>
      </div>

      <div class="piece__info">
        <p class="piece__name">Turquoise Hand Embroidered Kanchli</p>
        <p class="piece__craft">Kutchi hand-embroidery</p>
        <p class="piece__price">₹ 25,000</p>
        <span class="piece__swatches" aria-hidden="true">
          <i style="background: #1f7a8c"></i><i style="background: #c7a56a"></i><i style="background: #f2ede0"></i>
        </span>
      </div>
    </a>
    <a class="piece" href="/pages/shop-shrujan#catalogue">
      <div class="piece__media">
        <img src="{{ 'sj-megenta-pink-hand-embroidered-kanchli-1.webp' | asset_url }}" alt="Model wearing the megenta pink hand embroidered kanchli" loading="lazy">
        <span class="piece__badge">Sold out</span>
        <button class="piece__wish" type="button" aria-label="Save Megenta Pink Hand Embroidered Kanchli to wishlist">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>
        </button>
        <span class="piece__quick">Add to bag</span>
      </div>

      <div class="piece__info">
        <p class="piece__name">Megenta Pink Hand Embroidered Kanchli</p>
        <p class="piece__craft">Kutchi hand-embroidery</p>
        <p class="piece__price">₹ 25,000</p>
        <span class="piece__swatches" aria-hidden="true">
          <i style="background: #a8306a"></i><i style="background: #c7a56a"></i><i style="background: #f2ede0"></i>
        </span>
      </div>
    </a>
    <a class="piece" href="/pages/shop-shrujan#catalogue">
      <div class="piece__media">
        <img src="{{ 'sj-beige-hand-embroidered-kanchli-1.webp' | asset_url }}" alt="Model wearing the beige hand embroidered kanchli" loading="lazy">
        <button class="piece__wish" type="button" aria-label="Save Beige Hand Embroidered Kanchli to wishlist">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>
        </button>
        <span class="piece__quick">Add to bag</span>
      </div>

      <div class="piece__info">
        <p class="piece__name">Beige Hand Embroidered Kanchli</p>
        <p class="piece__craft">Kutchi hand-embroidery</p>
        <p class="piece__price">₹ 28,000</p>
        <span class="piece__swatches" aria-hidden="true">
          <i style="background: #c9b79c"></i><i style="background: #c7a56a"></i><i style="background: #f2ede0"></i>
        </span>
      </div>
    </a>
  </div>

  <div class="curated__more" data-reveal>
    <a class="btn btn--ghost" href="{{ section.settings.more_url | default: '/pages/shop-shrujan#catalogue' }}">
      {{ section.settings.more_label }} <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12h15"></path><path d="m13.5 6.5 5.5 5.5-5.5 5.5"></path></svg>
    </a>
  </div>

  <div class="curated__promises" data-reveal-child>
    {%- for block in section.blocks %}
    <div class="promise" {{ block.shopify_attributes }}>
      <p class="promise__title">{{ block.settings.title }}</p>
      <p class="promise__copy">{{ block.settings.copy }}</p>
    </div>
    {%- endfor %}
  </div>
</section>

<script>
(function () {
  var root = document.getElementById('curated');
  if (!root || root.__shrujanInit) return;
  root.__shrujanInit = true;

  var FILTERS = ['All', 'Bridal', 'Festive', 'Everyday', 'Gifting'];
  var grid = root.querySelector('.curated__grid');
  var buttons = root.querySelectorAll('.curated__filter');
  var filter = 'All';

  var HEART =
    '<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M12 19.8S4.5 15.3 4.5 9.9c0-2.6 2-4.4 4.2-4.4 1.5 0 2.7.8 3.3 2 .6-1.2 1.8-2 3.3-2 2.2 0 4.2 1.8 4.2 4.4 0 5.4-7.5 9.9-7.5 9.9Z"></path></svg>';

  function pieceHTML(p) {
    var badge = p.badge ? '<span class="piece__badge">' + p.badge + '</span>' : '';
    var was = p.was ? '<span>' + p.was + '</span>' : '';
    var swatches = p.swatches
      .map(function (c) { return '<i style="background: ' + c + '"></i>'; })
      .join('');
    return (
      '<a class="piece" href="/pages/shop-shrujan#catalogue">' +
        '<div class="piece__media">' +
          '<img src="' + window.SHRUJAN.asset(p.img) + '" alt="' + p.alt + '" loading="lazy">' +
          badge +
          '<button class="piece__wish" type="button" aria-label="Save ' + p.name + ' to wishlist">' + HEART + '</button>' +
          '<span class="piece__quick">Add to bag</span>' +
        '</div>' +
        '<div class="piece__info">' +
          '<p class="piece__name">' + p.name + '</p>' +
          '<p class="piece__craft">' + p.craft + '</p>' +
          '<p class="piece__price">' + p.price + was + '</p>' +
          '<span class="piece__swatches" aria-hidden="true">' + swatches + '</span>' +
        '</div>' +
      '</a>'
    );
  }

  function renderGrid() {
    var pieces = window.SHRUJAN.PIECES;
    var shown = (filter === 'All'
      ? pieces
      : pieces.filter(function (p) { return p.tag === filter; })
    ).slice(0, 5);
    grid.innerHTML = shown.map(pieceHTML).join('');
  }

  function setFilter(f) {
    if (f === filter) return;
    filter = f;
    buttons.forEach(function (btn, i) {
      var active = FILTERS[i] === filter;
      btn.classList.toggle('is-active', active);
      btn.setAttribute('aria-selected', active ? 'true' : 'false');
    });
    renderGrid();
  }

  buttons.forEach(function (btn, i) {
    btn.addEventListener('click', function () { setFilter(FILTERS[i]); });
  });

  /* the wishlist heart sits inside the card link — keep its click from navigating */
  root.addEventListener('click', function (e) {
    if (e.target.closest && e.target.closest('.piece__wish')) e.preventDefault();
  });
})();
</script>
{% schema %}
{
  "name": "Curated pieces",
  "settings": [
    {
      "type": "header",
      "content": "Intro"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Handpicked for you"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Curated pieces, chosen with care"
    },
    {
      "type": "textarea",
      "id": "intro",
      "label": "Intro copy",
      "default": "Timeless silhouettes, handcrafted details, edited by our Bhuj studio for the moments that matter."
    },
    {
      "type": "header",
      "content": "Filter tabs"
    },
    {
      "type": "paragraph",
      "content": "Labels only. Filtering still uses the built-in occasion categories, in this fixed order: All, Bridal, Festive, Everyday, Gifting."
    },
    {
      "type": "text",
      "id": "tab_1",
      "label": "Tab 1 label",
      "default": "All"
    },
    {
      "type": "text",
      "id": "tab_2",
      "label": "Tab 2 label",
      "default": "Bridal"
    },
    {
      "type": "text",
      "id": "tab_3",
      "label": "Tab 3 label",
      "default": "Festive"
    },
    {
      "type": "text",
      "id": "tab_4",
      "label": "Tab 4 label",
      "default": "Everyday"
    },
    {
      "type": "text",
      "id": "tab_5",
      "label": "Tab 5 label",
      "default": "Gifting"
    },
    {
      "type": "header",
      "content": "Call to action"
    },
    {
      "type": "text",
      "id": "more_label",
      "label": "Button label",
      "default": "View the full edit"
    },
    {
      "type": "url",
      "id": "more_url",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "tr_cols",
      "label": "Cards per row (desktop)",
      "min": 1,
      "max": 8,
      "step": 1,
      "default": 5,
      "info": "Applies above 1080px. Narrower screens keep the theme’s own ladder — 3, then 2. Five cards are shown, so a different count leaves a short last row."
    },
    {
      "type": "range",
      "id": "tr_cols_m",
      "label": "Cards per row (mobile)",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 2,
      "info": "Applies below 720px. Left at 2, phones under 560px keep stacking one card per row."
    },
    {
      "type": "select",
      "id": "tr_ratio",
      "label": "Image shape",
      "default": "default",
      "info": "Theme default steps from 3:4 down to 5:6 and then 4:5 as the screen narrows. Any other choice holds at every size.",
      "options": [
        {
          "value": "default",
          "label": "Theme default"
        },
        {
          "value": "1/1",
          "label": "Square (1:1)"
        },
        {
          "value": "4/5",
          "label": "Portrait (4:5)"
        },
        {
          "value": "3/4",
          "label": "Portrait (3:4)"
        },
        {
          "value": "2/3",
          "label": "Tall (2:3)"
        }
      ]
    },
    {
      "type": "header",
      "content": "Card content"
    },
    {
      "type": "checkbox",
      "id": "tr_show_filters",
      "label": "Show occasion filters",
      "default": true,
      "info": "Without them the grid stays on its opening selection."
    },
    {
      "type": "checkbox",
      "id": "tr_show_badge",
      "label": "Show status badge",
      "default": true,
      "info": "The “Sold out” style flag over the image."
    },
    {
      "type": "checkbox",
      "id": "tr_show_price",
      "label": "Show price",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "tr_show_was",
      "label": "Show “was” price",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "tr_show_swatches",
      "label": "Show colour swatches",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "tr_show_wish",
      "label": "Show wishlist button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "tr_show_quick",
      "label": "Show quick-add on hover",
      "default": true
    },
    {
      "type": "header",
      "content": "Design"
    },
    {
      "type": "select",
      "id": "sec_width",
      "label": "Section width",
      "info": "Theme default keeps the shared page rail.",
      "default": "default",
      "options": [
        {
          "value": "default",
          "label": "Theme default"
        },
        {
          "value": "narrow",
          "label": "Narrow"
        },
        {
          "value": "wide",
          "label": "Wide"
        },
        {
          "value": "full",
          "label": "Full width"
        }
      ]
    },
    {
      "type": "range",
      "id": "sec_space_top",
      "label": "Extra top spacing",
      "min": -120,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "sec_space_bottom",
      "label": "Extra bottom spacing",
      "min": -120,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "sec_space_top_m",
      "label": "Extra top spacing (mobile)",
      "min": -120,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "sec_space_bottom_m",
      "label": "Extra bottom spacing (mobile)",
      "min": -120,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "color",
      "id": "sec_bg",
      "label": "Background",
      "info": "Leave empty to keep the section’s own background."
    },
    {
      "type": "color",
      "id": "sec_text",
      "label": "Text colour"
    },
    {
      "type": "color",
      "id": "sec_accent",
      "label": "Accent colour"
    },
    {
      "type": "checkbox",
      "id": "sec_animate",
      "label": "Animate on scroll",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sec_hide_mobile",
      "label": "Hide on mobile",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "sec_hide_desktop",
      "label": "Hide on desktop",
      "default": false
    },
    {
      "type": "textarea",
      "id": "sec_custom_css",
      "label": "Custom CSS",
      "info": "Applies to this section only, e.g. .hero__title { letter-spacing: 2px; }"
    }
  ],
  "blocks": [
    {
      "type": "promise",
      "name": "Promise",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Complimentary shipping"
        },
        {
          "type": "text",
          "id": "copy",
          "label": "Copy",
          "default": "On every order above ₹5,000"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Curated pieces",
      "blocks": [
        {
          "type": "promise",
          "settings": {
            "title": "Complimentary shipping",
            "copy": "On every order above ₹5,000"
          }
        },
        {
          "type": "promise",
          "settings": {
            "title": "14-day easy returns",
            "copy": "Unworn pieces, tags intact"
          }
        },
        {
          "type": "promise",
          "settings": {
            "title": "Signed by the artisan",
            "copy": "Every piece carries her name"
          }
        },
        {
          "type": "promise",
          "settings": {
            "title": "Made to measure",
            "copy": "Free alterations on request"
          }
        }
      ]
    }
  ]
}
{% endschema %}
